NEXT_PUBLIC_API_URL=http://localhost:8000

# API credentials are deliberately NOT configured here. Anything prefixed with
# NEXT_PUBLIC_ is inlined into the static export at build time and is therefore
# readable by every visitor of the deployed site. The app prompts for the
# username/password on first use instead and caches them in sessionStorage.
